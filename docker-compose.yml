version: '3.8'

services:
  agentlens:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentlens-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - NODE_ENV=production
