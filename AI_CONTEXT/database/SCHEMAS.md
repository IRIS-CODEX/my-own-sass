# SCHEMAS.md — Full Database Schemas & Types

## 🐘 Drizzle ORM Schema (`backend/db/schema.ts`)
See `backend/db/schema.ts` for full TypeScript schema definitions and Drizzle relations.

## 🔥 Firestore Blueprint Schema (`firebase-blueprint.json`)
See `firebase-blueprint.json` and `firestore.rules` for document schemas and security rule constraints.
